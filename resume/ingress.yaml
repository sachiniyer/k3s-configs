apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resume-ingress
  namespace: website
spec:
  ingressClassName: traefik
  rules:
  - host: sachiniyer.com
    http:
      paths:
      - backend:
          service:
            name: resume-service
            port:
              number: 80
        path: /resume
        pathType: Prefix
  tls:
  - secretName: website-cert
